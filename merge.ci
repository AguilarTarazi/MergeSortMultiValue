module merge {

  array [1D] Merge {
    entry Merge();
    entry void initPhase(int,int,int,int val[numElements],int,int);
    entry void startCompare(int);
    entry void requestSwap(int,int,int);
    //entry void acceptSwap(int val[numElements],int);
    //entry void denySwap(int val[numElements],int);
    entry void saveValue(int val[numElements],int,bool);
    entry void check();
    entry void listo();

  };

};
